<nav class="fixed top-0 left-0 w-full z-50 bg-transparent px-6 py-4 flex justify-between items-center">
    <img src="{{ asset('images/logo1.png') }}" alt="Logo Cinéma XYZ" class="w-40">
    <div class="space-x-6 hidden md:flex text-white">
        <a href="{{ path('app_index') }}" class="hover:text-yellow-500 transition-all">Accueil</a>
        <a href="{{ path('app_reservation') }}" class="hover:text-yellow-500 transition-all">Réservation</a>
        <a href="{{path('app_films')}}" class="hover:text-yellow-500 transition-all">Films</a>
        <a href="{{ path('app_contact') }}" class="hover:text-yellow-500 transition-all">Contact</a>
        {% if app.user is null %}
            <a href="{{ path('app_login') }}" class="hover:text-yellow-500 transition-all">Connexion</a>
        {% else %}
        {% if 'ROLE_ADMIN' in app.user.roles %}
        <a href="{{ path('app_admin_dashboard') }}" class="hover:text-yellow-500 transition-all">Bonjour {{ app.user.firstname }}</a>
        {% elseif 'ROLE_USER' in app.user.roles %}
            <a href="{{ path('app_user_dashboard') }}" class="hover:text-yellow-500 transition-all">Bonjour {{ app.user.firstname }}</a>
        {% elseif 'ROLE_EMPLOYEE' in app.user.roles %}
            <a href="{{ path('app_employee_dashboard') }}" class="hover:text-yellow-500 transition-all">Bonjour {{ app.user.firstname }}</a>
        {% endif %}
        {% endif %}
    </div>
</nav>
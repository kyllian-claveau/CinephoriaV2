{% extends 'layouts/baseUser.html.twig' %}

{% block title %}Tableau de bord{% endblock %}

{% block content %}
    <div class="mt-24 md:ml-64 px-4 sm:px-6 lg:px-8">
        <!-- Titre principal -->
        <div class="text-center mb-8">
            <h1 class="text-4xl font-bold text-gray-800">Bienvenue sur votre Tableau de Bord</h1>
            <p class="text-gray-600 mt-2">Gérez vos réservations, explorez les films, et profitez des meilleures offres.</p>
        </div>

        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
            <div class="bg-white rounded-2xl shadow-md p-6">
                <h2 class="text-xl font-semibold text-gray-800 mb-4">Mes réservations</h2>
                <ul class="space-y-4">
                    {% for reservation in reservations %}
                    <li class="flex justify-between items-center">
                        <div>
                            <h3 class="text-lg font-medium text-gray-800">{{ reservation.session.film.title }}</h3>
                            <p class="text-gray-500">{{ reservation.session.startDate|date('d-m-Y') }}</p>
                        </div>
                        <button class="bg-yellow-500 text-white px-4 py-2 rounded-lg hover:bg-yellow-700">Détails</button>
                    </li>
                    {% endfor %}
                </ul>
            </div>

            <div class="bg-white rounded-2xl shadow-md p-6">
                <h2 class="text-xl font-semibold text-gray-800 mb-4">Films à venir</h2>
                <div class="grid grid-cols-2 gap-4">
                    {% for film in films %}
                    <div>
                        <img src="{{ asset("/images/film/"~ film.filmFilename) }}" alt="{{ film.title }}" class="rounded-lg">
                        <h3 class="text-lg font-medium mt-2">{{ film.title }}</h3>
                    </div>
                        {% else %}
                        <p>Aucun film ajouté</p>
                    {% endfor %}
                </div>
            </div>

            <div class="bg-white rounded-2xl shadow-md p-6">
                <h2 class="text-xl font-semibold text-gray-800 mb-4">Promotions</h2>
                <div class="text-gray-600">
                    <p class="mb-2">🎉 <strong>-20%</strong> pour tout les étudiants (sur place)</p>
                    <p>🎬 <strong>Offre spéciale :</strong> 2 places achetées, 1 offerte (sur place).</p>
                </div>
                <button class="mt-4 bg-yellow-500 text-white px-4 py-2 rounded-lg hover:bg-yellow-700">Voir les offres</button>
            </div>
        </div>

        <div class="bg-white rounded-2xl shadow-md p-6 mt-8">
            <h2 class="text-xl font-semibold text-gray-800 mb-4">Mes statistiques</h2>
            <div class="flex justify-between items-center">
                <div>
                    <p class="text-gray-600">Total des réservations :</p>
                    <p class="text-2xl font-bold text-gray-800">{{ totalReservations }}</p>
                </div>
                <div>
                    <p class="text-gray-600">Films visionnés :</p>
                    <p class="text-2xl font-bold text-gray-800">{{ distinctFilmsCount }}</p>
                </div>
                <div>
                    <p class="text-gray-600">Points fidélité :</p>
                    <p class="text-2xl font-bold text-gray-800">À venir</p>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
